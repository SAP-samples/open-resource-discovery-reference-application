const apiName = 'ORD Document API V1'
const apiNamespace = 'open-resource-discovery'
const apiMajorVersion = 'v1'
const apiEntryPoint = `${apiNamespace}/${apiMajorVersion}`
const apiVersion = '1.0.0' // full semver API version

export const ordDocumentApiV1Config = {
  apiName,
  apiNamespace,
  apiMajorVersion,
  apiEntryPoint,
  apiVersion,
}
