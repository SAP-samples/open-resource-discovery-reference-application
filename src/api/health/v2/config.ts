const apiName = 'Health Check API'
const apiNamespace = 'health'
const apiMajorVersion = 'v2'
const apiEntryPoint = `${apiNamespace}/${apiMajorVersion}`
const apiVersion = '2.0.0' // full semver API version

export const healthCheckV2Config = {
  apiName,
  apiNamespace,
  apiMajorVersion,
  apiEntryPoint,
  apiVersion,
}
